name: Build iOS
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download Godot
      run: |
        wget https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_macos.universal.zip
        unzip Godot_v4.7.1-stable_macos.universal.zip -d godot
        sudo mv godot/Godot.app /Applications/Godot.app
        sudo ln -s /Applications/Godot.app/Contents/MacOS/Godot /usr/local/bin/godot

    - name: Download Godot Export Templates
      run: |
        mkdir -p ~/Library/Application\ Support/Godot/export_templates/4.7.1.stable
        cd /tmp
        wget https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_export_templates.tpz
        unzip Godot_v4.7.1-stable_export_templates.tpz
        mv templates/* ~/Library/Application\ Support/Godot/export_templates/4.7.1.stable/

    - name: Create build directory
      run: mkdir -p build

    - name: Export iOS project
      run: godot --headless --export-release "iOS" build/comicshopcornerbrawl.ipa
      env:
        DISPLAY: :99.0

    - name: Set iOS Deployment Target
      run: sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 10.0;/IPHONEOS_DEPLOYMENT_TARGET = 12.0;/' build/comicshopcornerbrawl.xcodeproj/project.pbxproj

    - name: Build iOS project
      run: |
        xcodebuild -project build/comicshopcornerbrawl.xcodeproj \
          -scheme comicshopcornerbrawl \
          -configuration Development \
          -archivePath build/comicshopcornerbrawl.xcarchive \
          archive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO

    - name: Export IPA
      run: |
        cd build/your_project_name.xcarchive/Products/Applications
        mkdir Payload
        mv your_project_name.app Payload/
        zip -r App.ipa Payload/
        mv App.ipa ../../../

    - name: Upload IPA to GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Ipa
        path: build/App.ipa